// ? JSON, Array method, Array, DOM, Select DOM elements

// ? JSON
// const user = {
//     name: "Akhatmirzo",
//     isMarried: false,
//     age: 18
// }

// console.log(user);

// const userStr = JSON.stringify(user);

// console.log(userStr);

// const userConverted = JSON.parse(userStr);

// ? Array 
// const users = [
//     true,
//     "Eshmat",
//     20,
//     {friend: "Bobur", tools: ["Bolta", "Ketmon", "Tesha"]}
// ];

// ? Array methid
const arr = []
// ? Push
arr.push("Eshmat");
arr.push("Ismoil");
console.log(arr);

// const returned = array.pop();
// arr.pop();
// console.log(returned, arr);

// ? unShift
arr.unshift("Eshmat2");
console.log(arr);

// ? shift
// arr.shift();
arr.shift();
console.log(arr);